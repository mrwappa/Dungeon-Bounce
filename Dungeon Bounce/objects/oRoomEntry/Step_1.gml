/// @description
event_inherited();


/*var hover = mouse_x > x and mouse_x < x + sprite_width and 
			mouse_y > y and mouse_y < y + sprite_height;
		
image_blend = c_white;
if(hover)
{
	image_blend = c_red;	
}

if(hover and mouse_check_button(mb_left))
{
	x += mouse_x - prevMX;
	y += mouse_y - prevMY;
}

prevMX = mouse_x;
prevMY = mouse_y;*/